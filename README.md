# WordPress Starter via Docker

This repo is a starter package for launching WordPress via Docker.

## Running

Download repo. Ensure Docker Desktop is installed and running.

```
$ docker-compose up
```